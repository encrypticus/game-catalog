import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
  root: {

  },
  title: {
    fontWeight: 'bold',
  },
  field: {
    padding: 5,
    fontSize: 'inherit',
  },
  item: {
    fontSize: '1.2rem',
  },
});
